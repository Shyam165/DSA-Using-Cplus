#include "iostream"
#include <string>
using namespace std;
string grammar(string s, int count, int n)
{
    for (int i = 0; i < s.size(); i++)
    {
        if (s[i] == 0 && count <= n)
        {
        }
    }
}
int main()
{
    int n;
    cout << "enter the row number";
    cin >> n;
    int count = 0;
    grammar("0", count, n);
}